# MGO Tutorial

This is the source for the MGO Tutorial site. Build and deploy the site with [Hugo](https://gohugo.io/).

For development, run the following command in the terminal:

```bash
hugo server
```

This starts Hugo's development server (on port 1313 by default) and features hot-reloading.
